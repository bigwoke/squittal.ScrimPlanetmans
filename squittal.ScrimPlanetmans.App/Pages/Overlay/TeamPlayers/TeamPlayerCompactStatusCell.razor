@using squittal.ScrimPlanetmans.ScrimMatch.Models;

@if (Player != null)
{
    <TeamPlayerCompactStatusCellDetails Player=Player NoScore=NoScore CurrentRoundOnly=CurrentRoundOnly />
}

@code {
    [Parameter]
    public Player Player { get; set; }

    [Parameter]
    public bool NoScore { get; set; }

    [Parameter]
    public bool CurrentRoundOnly { get; set; }
}
